import logo from './logo.svg';
import './App.css';

// 최상위 컴포넌트 정의
function App() {
  return (
    <div>
      <h1>Hello, React</h1>
      <p>JSX 를 사용한 React 컴포넌트</p>
    </div>
  );
}

// 모듈 export 필수
// 이거 안 쓸 거면 function 앞에 export 붙이기
export default App;
